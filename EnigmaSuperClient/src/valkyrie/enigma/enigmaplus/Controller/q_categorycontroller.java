/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package valkyrie.enigma.enigmaplus.Controller;



/**
 *
 * @author hesh
 */
public class q_categorycontroller {
     // public static int addQuestion(String[] category) throws ClassNotFoundException, SQLException {
    // Connection conn = DBConnection.getDBConnection().getConnection();
    // Statement stm = conn.createStatement();
    // long qid, String question, String title, String q_short, int rate, String date, String attachment, String picture, String code, long uid) 

     //   String sql = "Insert into question(question,title,q_short,rate,date,attachment,picture,code,uid) values('" + questions.getQuestion()+ "','" +questions.getTitle()+ "','" + questions.getQ_short()+ "','" + questions.getRate()+ "','" + questions.getDate()+ "','" + questions.getAttachment()+ "','" + questions.getPicture()+ "','" + questions.getCode()+ "','" + questions.getUid()+ "')";
    //   int res = stm.executeUpdate(sql);
    //    return res;
    // }
}//
